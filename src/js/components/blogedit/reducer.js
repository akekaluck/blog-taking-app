import * as Actions from './actions';

const initState = {
  title: undefined,
  date: undefined,
  content: undefined
}

const ErrorText = (state = initState, action, stateb) => {
  switch (action.type) {
    default:
      return state;
  }
}

export default ErrorText;
